<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pollkadotv2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.PollKadotv2">
        <activity android:name=".Verification"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".ProfileActivityNavBar"
            android:label="@string/title_activity_profile_nav_bar"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".NewPoll"
            android:label="New Poll Creator" />
        <activity android:name=".AboutUs"
            android:label="AJAY + MIKHAYLA" />
        <activity android:name=".PollCount" />
    </application>

</manifest>